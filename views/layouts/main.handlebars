<!DOCTYPE html>
<html lang="en">

<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Qcent: Blog Site</title>

    <link rel="stylesheet" href="/stylesheets/style.css">
</head>

<body>
    <div class="wrapper">
        <header>
            <nav>
                <a href="/">Home</a>
                {{#if session.loggedIn}}
                <a href="/dashboard">Dashboard</a>
                <a href='#' id="logout" class="btn-no-style">Logout</a>
                {{else}}
                <a href="/login">Login</a>
                {{/if}}
            </nav>
            <div>
                <h1>
                    <a class='title' href="/">The Blog Site</a>
                </h1>
                {{#if session.loggedIn}}
                Welcome {{session.username}}
                {{/if}}
            </div>
        </header>
        <main>
            {{{ body }}}
        </main>
        <footer>
            Blog On {{session.username}}!
        </footer>
    </div>
    {{#if session.loggedIn}}
    <script src="/javascript/logout.js"></script>

    {{/if}}
</body>

</html>